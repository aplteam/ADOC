 R←Test_Usercommand_03(stopFlag batchFlag);newdoc;buffer;flag;p;sep;⎕TRAP;filename;html;expected;more
⍝ Test the ADOC user command with a "browse" and -browser=
 R←∆Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 :Trap 11 911
     more←⎕SE.UCMD'Adoc #._ADOC.ADOC -browser=MyBrowser.exe'
     →FailsIf 1
 :Else
     →FailsIf 0=+/'DOMAIN ERROR'⍷⎕DMX.EM    ⍝ Should fail because no MyBrowse.exe exists...
 :EndTrap

 R←∆OK
