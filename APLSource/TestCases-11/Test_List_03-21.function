 R←Test_List_03(stopFlag batchFlag);⎕TRAP;buff
⍝ `List` a sub class that carries a single property definition for multiple properties.
 R←∆Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 ##.ADOC.RestoreDefaults

 buff←##.ADOC.List ##.ADOC.Demo
 →PassesIf∨/'Hello'⍷buff

 R←∆OK
